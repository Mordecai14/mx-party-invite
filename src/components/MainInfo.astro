---

---

<div>
    <div class="w-full text-center mt-10">
        <div class="text-2xl">
            <h2>
                No time to siesta and... <br />
            </h2>
            <h2
                class="animate-charcter text-4xl mt-2"
            >
                VIVA LA FIESTA!
            </h2>
            <h1 class="mt-10">Acompañanos a Celebrear el cumpleaños de:</h1>
            <img
                src="../../public/assets/farith_logo.png"
                alt="Farith"
                class="p-0 -mt-10"
            />
            <div></div>
        </div>

        <style>
            .animate-charcter {
            text-transform: uppercase;
            background-image: linear-gradient(
                -225deg,
                #0fc80c 0%,
                #ffffff 15%,
                #ff1361 88%,
                #0fc80c 100%
            );
            background-size: auto auto;
            background-clip: border-box;
            background-size: 200% auto;
            color: #fff;
            background-clip: text;
            text-fill-color: transparent;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: textclip 2s linear infinite, jump 1s ease-in-out infinite;
            display: inline-block;
        }

        @keyframes textclip {
            to {
                background-position: 200% center;
            }
        }

        @keyframes jump {
            0%, 100% {
                transform: scale(0.9);
            }
            50% {
                transform: scale(1.2);
            }
        }
        </style>
    </div>
</div>
